#include "main.h"
/**
 *_memcpy - copy memory area
 *@dest: store memory
 *@src: copied memory
 *@n: number of bytes
 *Return: copied memory
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{

for (int q = 0, int p = n; q < i; q++)
{
dest[q] = src[q];
n--;
}
return (dest);
}
