#include <stdio.h>

/**
 * main - prints all arguments
 * @argc: counter of argument
 * @argv: value of argument
 * Return: 0
 */
int main(int argc, char argv)
{

int xy;

while (argc--)
{
printf("%s\n", argv[xy])
xy++;

}

return (0);
}
